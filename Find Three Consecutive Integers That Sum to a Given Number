class Solution:
    def sumOfThree(self, num: int) -> List[int]: 
        if num % 3 != 0:
            return []
        answer = []
        for n in range(num//3 - 1 , num//3 + 2):
            answer.append(n)
        return answer
        
