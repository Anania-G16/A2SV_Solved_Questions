class Solution:
    def topKFrequent(self, nums: List[int], k: int) -> List[int]:
        freq = dict()
        freqList = []
        wanted = []
        answer = []
        for n in nums:
            if n not in freq:                        
                freq[n] = 1
            else:
                freq[n] += 1
        for value in freq.values():
            freqList.append(value)

        freqList.sort(reverse = True)
        
        for i in range(k):
            wanted.append(freqList[i])

        for key, value in freq.items():
            if value in wanted:
                answer.append(key)

        return answer

        
