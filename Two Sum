class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        myDict =  dict()
        for i  in range(len(nums)):
            wanted = target - nums[i]
            if wanted in myDict:
                return [myDict[wanted], i]
            myDict[nums[i]] = i
        return []
